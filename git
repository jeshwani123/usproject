Task 1 – Git Commands for Uploading Multi-Folder Project
git init
git remote add origin https://github.com/yourusername/your-private-repo.git
git add .
git commit -m "Initial commit"
git push -u origin master
git push -u origin main

Task 2 – All Git Commands (Full List)
1. Git Version & Config
git --version
git config --global user.name "Your Name"
git config --global user.email "youremail@example.com"
git config --list

2. Repository Management
git init
git clone https://github.com/username/repository.git
git remote -v
git remote add origin https://github.com/username/repository.git
git push origin main
git pull origin main
git remote remove origin
git remote rename origin upstream
git fetch origin
git remote set-url origin https://github.com/username/new-repository.git
git remote show origin

3. Staging & Committing
git status
git add filename.txt
git add .
git commit -m "Commit message"

4. Branching & Merging
git branch
git branch -a
git branch branch-name
git branch -d branch-name
git branch -D branch-name
git branch -r
git checkout branch-name
git checkout -b new-branch
git remote prune origin
git fetch origin feature-branch
git push --set-upstream origin feature-branch
git checkout main
git merge branch-name
git fetch origin
git rebase origin/main

5. Undoing Changes
git reset <file>
git revert <commit>

6. Viewing History
git log
git log --oneline
git diff
git show <commit>

7. Restoring File Before Staging
git restore filename

8. Fix Wrong Commit Message
git commit --amend -m "Corrected commit message"

9. Recover Deleted Branch
git reflog
git checkout -b feature-ui <commit_hash>

10. Download Latest Changes Without Merge
git fetch origin

11. Remove Sensitive Files From Git History
git filter-branch --force --index-filter "git rm --cached --ignore-unmatch secrets.txt" \
--prune-empty --tag-name-filter cat -- --all

12. Merge Changes From Another Branch
git checkout previous-branch
git merge another-branch

13. Resolve Merge Conflicts
git add app.js
git commit

14. .gitignore
touch .gitignore

15. Find Who Edited Each Line
git blame script.py

16. Git Stash
git stash
git switch another-branch
git switch main
git stash apply

17. Check If Branch Is Merged
git branch --merged

18. Delete Multiple Local Branches
git branch -d branch1 branch2 branch3
